[project]
name = "nsekit-mcp"
version = "0.0.30"
description = "FastMCP server for NSE India market data using NseKit"
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
    "mcp[cli]>=2.0.0",
    "nsekit>=0.0.42",
    "typer>=0.20.0",
]

[project.scripts]
nsekit-mcp = "nsekit_mcp.server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
